# Front-End

The active Expo app lives in `front-end/mobile`.

## Run the mobile app

From this folder:

```bash
npm run start
npm run android
npm run ios
npm run web
npm run lint
```

These scripts proxy to the real app in `front-end/mobile`.
